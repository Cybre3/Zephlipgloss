import React from 'react';
export default React.createContext(null);

// import { createContext } from "react";

// const authContext = createContext({
//   authenticated: false,
//   setAuthenticated: (auth) => console.log("Working!")
// });

// export default authContext;